// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// A subscription represents a subscribers' access to a particular set of
/// published data. It contains references to associated listings,
/// data exchanges, and linked datasets.
public struct Subscription: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Output only. The resource name of the subscription.
  /// e.g. `projects/myproject/locations/us/subscriptions/123`.
  public var name: Swift.String = Swift.String()

  /// Output only. Timestamp when the subscription was created.
  public var creationTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Timestamp when the subscription was last modified.
  public var lastModifyTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Organization of the project this subscription belongs to.
  public var organizationId: Swift.String = Swift.String()

  /// Output only. Display name of the project of this subscription.
  public var organizationDisplayName: Swift.String = Swift.String()

  /// Output only. Current state of the subscription.
  public var state: Subscription.State = Subscription.State()

  /// Output only. Map of listing resource names to associated linked resource,
  /// e.g. projects/123/locations/us/dataExchanges/456/listings/789
  /// ->
  /// projects/123/datasets/my_dataset
  ///
  /// For listing-level subscriptions, this is a map of size 1.
  /// Only contains values if state == STATE_ACTIVE.
  public var linkedDatasetMap: [Swift.String: Subscription.LinkedResource] = [:]

  /// Output only. Email of the subscriber.
  public var subscriberContact: Swift.String = Swift.String()

  /// Output only. Linked resources created in the subscription. Only contains
  /// values if state = STATE_ACTIVE.
  public var linkedResources: [Subscription.LinkedResource] = []

  /// Output only. Listing shared asset type.
  public var resourceType: SharedResourceType = SharedResourceType()

  /// Output only. This is set if this is a commercial subscription i.e. if this
  /// subscription was created from subscribing to a commercial listing.
  public var commercialInfo: Subscription.CommercialInfo? = nil

  /// Output only. By default, false.
  /// If true, the Subscriber agreed to the email sharing mandate
  /// that is enabled for DataExchange/Listing.
  public var logLinkedDatasetQueryUserEmail: Swift.Bool? = nil

  /// Optional. BigQuery destination dataset to create for the subscriber.
  public var destinationDataset: DestinationDataset? = nil

  public var resourceName: OneOf_ResourceName? = nil

  /// Initialize a new instance of `Subscription`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Subscription().with { $0.listing = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: String, CodingKey {
    case listing = "listing"
    case dataExchange = "dataExchange"
    case name = "name"
    case creationTime = "creationTime"
    case lastModifyTime = "lastModifyTime"
    case organizationId = "organizationId"
    case organizationDisplayName = "organizationDisplayName"
    case state = "state"
    case linkedDatasetMap = "linkedDatasetMap"
    case subscriberContact = "subscriberContact"
    case linkedResources = "linkedResources"
    case resourceType = "resourceType"
    case commercialInfo = "commercialInfo"
    case logLinkedDatasetQueryUserEmail = "logLinkedDatasetQueryUserEmail"
    case destinationDataset = "destinationDataset"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.creationTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .creationTime)
    self.lastModifyTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .lastModifyTime)
    self.organizationId = try container.decode(Swift.String.self, forKey: .organizationId)
    self.organizationDisplayName = try container.decode(
      Swift.String.self, forKey: .organizationDisplayName)
    self.state = try container.decode(Subscription.State.self, forKey: .state)
    self.linkedDatasetMap = try container.decode(
      [Swift.String: Subscription.LinkedResource].self, forKey: .linkedDatasetMap)
    self.subscriberContact = try container.decode(Swift.String.self, forKey: .subscriberContact)
    self.linkedResources = try container.decode(
      [Subscription.LinkedResource].self, forKey: .linkedResources)
    self.resourceType = try container.decode(SharedResourceType.self, forKey: .resourceType)
    self.commercialInfo = try container.decodeIfPresent(
      Subscription.CommercialInfo.self, forKey: .commercialInfo)
    self.logLinkedDatasetQueryUserEmail = try container.decodeIfPresent(
      Swift.Bool.self, forKey: .logLinkedDatasetQueryUserEmail)
    self.destinationDataset = try container.decodeIfPresent(
      DestinationDataset.self, forKey: .destinationDataset)

    var resourceName: OneOf_ResourceName? = nil
    let resourceNameCheckAndSet = {
      if resourceName != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'resourceName'"))
      }
      resourceName = $0
    }
    if let listing = try container.decodeIfPresent(Swift.String.self, forKey: .listing) {
      try resourceNameCheckAndSet(.listing(listing))
    }
    if let dataExchange = try container.decodeIfPresent(Swift.String.self, forKey: .dataExchange) {
      try resourceNameCheckAndSet(.dataExchange(dataExchange))
    }
    self.resourceName = resourceName
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.creationTime, forKey: .creationTime)
    try container.encode(self.lastModifyTime, forKey: .lastModifyTime)
    try container.encode(self.organizationId, forKey: .organizationId)
    try container.encode(self.organizationDisplayName, forKey: .organizationDisplayName)
    try container.encode(self.state, forKey: .state)
    try container.encode(self.linkedDatasetMap, forKey: .linkedDatasetMap)
    try container.encode(self.subscriberContact, forKey: .subscriberContact)
    try container.encode(self.linkedResources, forKey: .linkedResources)
    try container.encode(self.resourceType, forKey: .resourceType)
    try container.encode(self.commercialInfo, forKey: .commercialInfo)
    try container.encode(
      self.logLinkedDatasetQueryUserEmail, forKey: .logLinkedDatasetQueryUserEmail)
    try container.encode(self.destinationDataset, forKey: .destinationDataset)

    if let choice = self.resourceName {
      switch choice {
      case .listing(let value):
        try container.encode(value, forKey: .listing)
      case .dataExchange(let value):
        try container.encode(value, forKey: .dataExchange)
      }
    }
  }

  /// Reference to a linked resource tracked by this Subscription.
  public struct LinkedResource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. Listing for which linked resource is created.
    public var listing: Swift.String = Swift.String()

    public var reference: OneOf_Reference? = nil

    /// Initialize a new instance of `LinkedResource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = LinkedResource().with { $0.linkedDataset = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: String, CodingKey {
      case linkedDataset = "linkedDataset"
      case linkedPubsubSubscription = "linkedPubsubSubscription"
      case listing = "listing"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.listing = try container.decode(Swift.String.self, forKey: .listing)

      var reference: OneOf_Reference? = nil
      let referenceCheckAndSet = {
        if reference != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'reference'"))
        }
        reference = $0
      }
      if let linkedDataset = try container.decodeIfPresent(
        Swift.String.self, forKey: .linkedDataset)
      {
        try referenceCheckAndSet(.linkedDataset(linkedDataset))
      }
      if let linkedPubsubSubscription = try container.decodeIfPresent(
        Swift.String.self, forKey: .linkedPubsubSubscription)
      {
        try referenceCheckAndSet(.linkedPubsubSubscription(linkedPubsubSubscription))
      }
      self.reference = reference
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.listing, forKey: .listing)

      if let choice = self.reference {
        switch choice {
        case .linkedDataset(let value):
          try container.encode(value, forKey: .linkedDataset)
        case .linkedPubsubSubscription(let value):
          try container.encode(value, forKey: .linkedPubsubSubscription)
        }
      }
    }

    public enum OneOf_Reference: Codable, Equatable, Sendable {
      /// Output only. Name of the linked dataset, e.g.
      /// projects/subscriberproject/datasets/linked_dataset
      case linkedDataset(Swift.String)
      /// Output only. Name of the Pub/Sub subscription, e.g.
      /// projects/subscriberproject/subscriptions/subscriptions/sub_id
      case linkedPubsubSubscription(Swift.String)
    }

    public static var _anyTypeUrl: String {
      return "type.googleapis.com/google.cloud.bigquery.analyticshub.v1.Subscription.LinkedResource"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Commercial info metadata for this subscription.
  public struct CommercialInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. This is set when the subscription is commercialised via
    /// Cloud Marketplace.
    public var cloudMarketplace: Subscription.CommercialInfo.GoogleCloudMarketplaceInfo? = nil

    /// Initialize a new instance of `CommercialInfo`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CommercialInfo().with { $0.cloudMarketplace = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Cloud Marketplace commercial metadata for this subscription.
    public struct GoogleCloudMarketplaceInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Resource name of the Marketplace Order.
      public var order: Swift.String = Swift.String()

      /// Initialize a new instance of `GoogleCloudMarketplaceInfo`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = GoogleCloudMarketplaceInfo().with { $0.order = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: String {
        return
          "type.googleapis.com/google.cloud.bigquery.analyticshub.v1.Subscription.CommercialInfo.GoogleCloudMarketplaceInfo"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: String {
      return "type.googleapis.com/google.cloud.bigquery.analyticshub.v1.Subscription.CommercialInfo"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// State of the subscription.
  public enum State: Codable, Equatable, Sendable {
    /// Default value. This value is unused.
    case unspecified
    /// This subscription is active and the data is accessible.
    case active
    /// The data referenced by this subscription is out of date and should be
    /// refreshed. This can happen when a data provider adds or removes datasets.
    case stale
    /// This subscription has been cancelled or revoked and the data is no longer
    /// accessible.
    case inactive
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .active: return 1
      case .stale: return 2
      case .inactive: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: String? {
      switch self {
      case .unspecified: return "STATE_UNSPECIFIED"
      case .active: return "STATE_ACTIVE"
      case .stale: return "STATE_STALE"
      case .inactive: return "STATE_INACTIVE"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
    public init(stringValue: String) {
      switch stringValue {
      case "STATE_UNSPECIFIED": self = .unspecified
      case "STATE_ACTIVE": self = .active
      case "STATE_STALE": self = .stale
      case "STATE_INACTIVE": self = .inactive
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .active
      case 2: self = .stale
      case 3: self = .inactive
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .active: return try container.encode(1)
      case .stale: return try container.encode(2)
      case .inactive: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public enum OneOf_ResourceName: Codable, Equatable, Sendable {
    /// Output only. Resource name of the source Listing.
    /// e.g. projects/123/locations/us/dataExchanges/456/listings/789
    case listing(Swift.String)
    /// Output only. Resource name of the source Data Exchange.
    /// e.g. projects/123/locations/us/dataExchanges/456
    case dataExchange(Swift.String)
  }

  public static var _anyTypeUrl: String {
    return "type.googleapis.com/google.cloud.bigquery.analyticshub.v1.Subscription"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
